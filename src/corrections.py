# src/corrections.py  — full body arrives in Session 4; this stub makes Session 3 runnable
def build_corrections_context(store, sender_ref: str, max_n: int) -> str:
    return ""   # no corrections yet; classifier runs without correction context